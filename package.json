{
  "name": "gamev1-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n web,server -c blue,green \"npm run dev:web\" \"npm run dev:server\"",
    "dev:web": "vite",
    "dev:server": "cd server && bun run dev",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4173"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^7.1.0"
  }
}
